/**
 * 获取执行该方法时所在的目录
 * @returns 当前工作目录的绝对路径
 */
declare const useGetCurrentPath: () => string;
/**
 * 判断当前路径是否是文件
 * @param path 要检查的路径
 * @returns 如果是文件返回true，否则返回false
 */
declare const useIsFile: (path: string) => boolean;
/**
 * 判断当前路径是否是目录
 * @param path 要检查的路径
 * @returns 如果是目录返回true，否则返回false
 */
declare const useIsDirectory: (path: string) => boolean;
type FileName = string | {
    name: string;
    suffix: string;
};
/**
 * 如果路径Path的是一个文件则会返回文件名和后缀名，否则返回目录名
 * @param path 要获取文件名的路径
 * @returns 文件名对象或目录名字符串
 */
declare const useGetFileName: (path: string) => FileName;
/**
 * 获取路径的后缀名
 * @param path 要获取后缀名的路径
 * @returns 文件后缀名（不包含点号）
 */
declare const useGetExtensionName: (path: string) => string;
interface ExecCommandOptions {
    cwd: string;
}
/**
 * 执行命令
 * @param command 要执行的命令
 * @param options 执行选项，包含工作目录
 */
declare const useExecCommand: (command: string, options?: ExecCommandOptions) => void;
/**
 * 递归删除目录
 * @param path 要删除的目录路径
 */
declare const useRecursionDelete: (path: string) => void;
/**
 * 获取文件的绝对路径
 * @param filename 文件名
 * @returns 文件的绝对路径
 */
declare const useGetFilePath: (filename: string) => string;
/**
 * 检查当前进程的标准输入是否是TTY（终端环境）
 * @returns 如果是TTY返回true，否则返回false
 */
declare const useIsTTY: () => boolean;

export { useExecCommand, useGetCurrentPath, useGetExtensionName, useGetFileName, useGetFilePath, useIsDirectory, useIsFile, useIsTTY, useRecursionDelete };
